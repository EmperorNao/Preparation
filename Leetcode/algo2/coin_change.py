
class Solution:

    def sum_up(self, coins, pos, sum, amount):

        cnt += 1
        while sum + amount < pos:






    def coinChange(self, coins: List[int], amount: int) -> int:

        self.m = [[-1 for i in range(amount)] for j in range(len(coins)))
        return self.sum_up(coins, 0)
